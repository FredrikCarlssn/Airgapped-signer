"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.importFilecoinERC20Tokens = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const fetch_1 = require("../../fetch");
const importFilecoinERC20Tokens = (outputDir) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        console.log("importing filecoin erc20 tokens...");
        const [erc20tokens, hash] = yield (0, fetch_1.fetchTokens)("evm/314/erc20.json");
        const filePath = path_1.default.join(outputDir, "filecoin-erc20");
        fs_1.default.writeFileSync(`${filePath}.json`, JSON.stringify(erc20tokens));
        if (hash) {
            fs_1.default.writeFileSync(`${filePath}-hash.json`, JSON.stringify(hash));
        }
        fs_1.default.writeFileSync(`${filePath}.ts`, `export type FilecoinERC20Token = [
  string, // parent currency id
  string, // token
  string, // ticker
  number, // precision
  string, // name
  string, // ledgerSignature
  string, // contract eth address
  //   string, // contract fil address
  boolean, // disabled counter values
  boolean, // delisted
  (string | null)?, // countervalue_ticker (legacy)
];

import tokens from "./filecoin-erc20.json";

${hash ? `export { default as hash } from "./filecoin-erc20-hash.json";` : ""}

export default tokens as FilecoinERC20Token[];
`);
        console.log("importing filecoin erc20 tokens sucess");
    }
    catch (err) {
        console.error(err);
    }
});
exports.importFilecoinERC20Tokens = importFilecoinERC20Tokens;
//# sourceMappingURL=index.js.map