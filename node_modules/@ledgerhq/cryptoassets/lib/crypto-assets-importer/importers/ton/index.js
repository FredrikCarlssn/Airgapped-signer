"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.importTonJettonTokens = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const fetch_1 = require("../../fetch");
const importTonJettonTokens = (outputDir) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        console.log("importing ton jetton tokens...");
        const [jettontokens, hash] = yield (0, fetch_1.fetchTokens)("jetton.json");
        const filePath = path_1.default.join(outputDir, "ton-jetton");
        fs_1.default.writeFileSync(`${filePath}.json`, JSON.stringify(jettontokens));
        if (hash) {
            fs_1.default.writeFileSync(`${filePath}-hash.json`, JSON.stringify(hash));
        }
        fs_1.default.writeFileSync(`${filePath}.ts`, `export type TonJettonToken = [
  string, // contractAddress
  string, // name
  string, // ticker
  number, // magntude
  boolean, // delisted
  boolean, // enableCountervalues
];

import tokens from "./ton-jetton.json";

${hash ? `export { default as hash } from "./ton-jetton-hash.json";` : ""}

export default tokens as TonJettonToken[];
`);
        console.log("importing ton jetton tokens success");
    }
    catch (err) {
        console.error(err);
    }
});
exports.importTonJettonTokens = importTonJettonTokens;
//# sourceMappingURL=index.js.map